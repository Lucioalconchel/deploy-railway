<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<navbar>
    <a href="#" id="Inicio"> Inicio </a> <br>
    <a href="#" id="NuevoUsuario">Nuevo Usuario || </a>
    <a href="#" id="Usuarios">Usuarios || </a>
    <a href="#" id="NuevoProducto">Nuevo Producto || </a>
    <a href="#" id="Productos">Productos || </a>
    <a href="#" id="NuevaCategoria">Nueva categoria || </a>
    <a href="#" id="Categorias">Categorias</a>
</navbar>
<body>
    <form action='/nuevoComentario' method="POST">
        <label for="">Usuarios</label>
        <select name="usuario">
            <% for (var i =0; i< usuarios.length; i++ ) { %>
                <option value= <%= usuarios[i].id_usuario %> > <%= usuarios[i].email %> </option>
            <% }%>
        </select>
        <input type="text" name="descripcion" placeholder="comentario">
        <input type="submit" value="Enviar">
   </form>
</body>
<script src="../funciones.js"></script>
</html>